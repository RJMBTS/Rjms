name: P1jc 🌐

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

concurrency:
  group: master-m3u
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build P1jc.m3u (OTT Navigator Compatible + CNBC Added)
        id: buildfile
        run: |
          set -e
          timestamp=$(TZ="Asia/Kolkata" date +"%Y-%m-%d %H:%M:%S %Z")
          tmpdir=$(mktemp -d)

          mkdir -p Bishop
          OUTPUT="Bishop/P1jc.m3u"
          JC_URL="https://raw.githubusercontent.com/alex8875/m3u/refs/heads/main/jcinema.m3u"

          echo "Fetching playlist..."
          curl -fsSL "$JC_URL" -o "$tmpdir/jc_raw.m3u"

          # Remove useless metadata
          grep -v -E '^#EXTM3U|#Generated on|# Join Telegram' "$tmpdir/jc_raw.m3u" > "$tmpdir/clean.m3u"

          # Remove any old group-title attributes
          sed -E 's/group-title="[^"]*"//g' "$tmpdir/clean.m3u" > "$tmpdir/tmp1.m3u"

          # Normalize spacing to avoid OTT Navigator parsing failures
          sed -E 's/[[:space:]]+/ /g' "$tmpdir/tmp1.m3u" > "$tmpdir/tmp2.m3u"

          # OTT Navigator fix — group-title must be FIRST attribute
          awk '
          /^#EXTINF/ {
              line=$0

              # Split EXTINF attributes from channel name
              split(line, parts, ",")
              attrs = parts[1]
              name  = parts[2]

              # Apply grouping to: News18 + CNBC channels
              if (name ~ /News18/ || name ~ /CNBC/) {

                  # Remove "#EXTINF:-1 " from attributes
                  gsub(/^#EXTINF:-1 */, "", attrs)

                  # Build new EXTINF line with group-title FIRST
                  new_line = "#EXTINF:-1 group-title=\"RJM | Viacom18\" " attrs "," name

                  print new_line
                  next
              }

              print line
              next
          }
          { print }
          ' "$tmpdir/tmp2.m3u" > "$tmpdir/final.m3u"

          {
            echo "#EXTM3U"
            echo "# Last updated: $timestamp"
          } > "$OUTPUT"

          cat "$tmpdir/final.m3u" >> "$OUTPUT"

          echo "file-ready=true" >> $GITHUB_OUTPUT

      - name: Commit and push
        if: steps.buildfile.outputs.file-ready == 'true'
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          git add Bishop/P1jc.m3u

          if ! git diff --cached --quiet; then
            git commit -m "P1jc OTT Navigator Group Fix (News18 + CNBC)"
            git pull --rebase origin main || true
            git push origin main || git push origin main
          else
            echo "No changes"
          fi
